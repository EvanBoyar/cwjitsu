# Build outputs
build/
app/build/

# Local SDK configuration
local.properties

# IDE
.idea/
*.iml
.gradle/
.kotlin/

# Signing keys must NEVER be committed - CI gets the keystore via an
# encrypted Actions secret, local builds use ~/.android/debug.keystore.
*.keystore
*.jks
